'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function AdminLogin(){const router=useRouter(),[error,setError]=useState(''),[loading,setLoading]=useState(false);return <form className="admin-login" onSubmit={async event=>{event.preventDefault();setLoading(true);setError('');const form=new FormData(event.currentTarget),response=await fetch('/api/admin/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:form.get('email'),password:form.get('password')})});setLoading(false);if(response.ok){router.push('/admin');router.refresh()}else setError('The email or password is incorrect.')}}><label>Email<input name="email" type="email" required autoComplete="username"/></label><label>Password<input name="password" type="password" required autoComplete="current-password"/></label><button disabled={loading}>{loading?'SIGNING IN…':'SIGN IN'}</button><p role="alert">{error}</p></form>}

