import {NextRequest,NextResponse} from 'next/server';export function proxy(request:NextRequest){if(!request.nextUrl.pathname.startsWith('/admin'))return NextResponse.next();if(request.nextUrl.pathname==='/admin/login'){const headers=new Headers(request.headers);headers.set('x-astreli-login-page','1');return NextResponse.next({request:{headers}})}const session=request.cookies.get('astreli_admin')?.value;if(!session)return NextResponse.redirect(new URL('/admin/login',request.url));return NextResponse.next()}export const config={matcher:['/admin/:path*']};
